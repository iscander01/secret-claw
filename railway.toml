[build]
dockerfilePath = "wizard/Dockerfile"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/api/_debug/storage"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
